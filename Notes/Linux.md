# A Note of Linux Learning
## file operation
    cd ~ cd ..
    mkdir rmdir rm file/dir -rf
    sudo chown user:usergroup file
    zip -r dir.zip dir & rar a -r newdir dir
    unzip dir.zip & unrar X newdir.rar
    